全局设置：
 下载并安装 Git
  git config --global user.name "Your Name"
  git config --global user.email yourname@youremail.com
        
下一步：
  mkdir ChiSeg
  cd ChiSeg
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:FLYui/ChiSeg.git
  git push -u origin master
      
现有的 Git 仓库？
  cd existing_git_repo
  git remote add origin git@github.com:FLYui/ChiSeg.git
  git push -u origin master

//backup only 
//抓取新浪的新闻作为数据源，进行统计分析，最后形成词库